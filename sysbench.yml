#database name
database: "sbtest"
#table name prefix
tableName: "sbtest"
#table count
tableCount: 10
#table record size
tableSize: 10000
# if table id is auto_incremental
autoIncrement: "false"
#batch size when loading data
batchSize: 10000